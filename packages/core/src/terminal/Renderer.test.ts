import { describe, it, expect } from 'vitest';

describe('Renderer', () => {
    it('placeholder test', () => {
        expect(true).toBe(true);
    });

    it('supports diff renderer mode', () => {
        expect(true).toBe(true);
    });
});